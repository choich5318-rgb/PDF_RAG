# 다중 PDF 처리 및 폴더 구조 개선 계획

## 📋 요구사항 분석

### 사용자 요구사항
1. ✅ **다중 PDF 처리**: 여러 PDF를 동시에 관리하고 질문 가능
2. ✅ **폴더별 구분**: 각 PDF의 벡터 저장소를 별도 폴더로 관리
3. ✅ **PDF 파일 정리**: 참조하는 PDF 파일들을 별도 폴더에 정리

---

## 🎯 구현 계획

### 1. 폴더 구조 개선

#### 현재 구조
```
PDF_RAG/
├── [PDF 파일들] (루트에 산재)
├── chroma_pdf_cache/ (단일 벡터 저장소)
└── Ollama_PDF_RAG.py
```

#### 개선 후 구조
```
PDF_RAG/
├── pdfs/                          # 📁 모든 PDF 파일 저장
│   ├── 20251212110152.pdf
│   ├── A Case Study with Locally Deployed Ollama Models .pdf
│   ├── samilpwc_industry-outlook2026.pdf
│   └── 삼정KPMG-AI에이전트-20250908.pdf.coredownload.inline.pdf
├── vector_cache/                  # 📁 벡터 저장소 루트
│   ├── {pdf_hash_1}/              # PDF별 벡터 DB (폴더 구분)
│   │   ├── chroma.sqlite3
│   │   └── ...
│   ├── {pdf_hash_2}/
│   └── metadata.json              # PDF 메타데이터 관리
├── Ollama_PDF_RAG.py              # 개선된 메인 코드
├── config.yaml                    # 설정 파일
└── requirements.txt
```

### 2. 주요 기능 개선

#### 2.1 다중 PDF 관리
- **PDF 목록 표시**: 업로드된 PDF 목록 확인
- **PDF 선택**: 질문할 PDF 선택 가능
- **PDF별 벡터 DB**: 각 PDF마다 독립적인 벡터 저장소
- **캐싱**: 파일명 + 수정시간 기반 스마트 캐싱

#### 2.2 폴더 구조 관리
- **PDF 파일 자동 정리**: `pdfs/` 폴더로 자동 이동
- **벡터 DB 분리**: PDF별 해시 기반 폴더 생성
- **메타데이터 관리**: PDF 정보 저장 (파일명, 경로, 수정시간 등)

#### 2.3 UI 개선
- **PDF 선택 드롭다운**: 등록된 PDF 목록에서 선택
- **PDF 업로드**: 새 PDF 추가 기능
- **PDF 정보 표시**: 선택된 PDF의 정보 표시

---

## 🔧 구현 상세 내용

### 3.1 새로운 클래스 및 함수

#### `PDFManager` 클래스
```python
class PDFManager:
    - PDF 파일 관리
    - PDF 목록 조회
    - PDF 메타데이터 관리
    - PDF 파일 이동 및 정리
```

#### `VectorStoreManager` 클래스
```python
class VectorStoreManager:
    - PDF별 벡터 저장소 생성/로드
    - 캐시 확인 (파일 수정시간 체크)
    - 벡터 DB 경로 관리
    - 메타데이터 저장/로드
```

#### 주요 함수 개선
- `load_pdf_list()`: pdfs/ 폴더의 PDF 목록 반환
- `get_or_create_vectorstore(pdf_path)`: PDF별 벡터 저장소 가져오기/생성
- `organize_pdf_files()`: 루트의 PDF를 pdfs/ 폴더로 이동

### 3.2 Gradio UI 개선

#### 새로운 UI 구성
```python
- PDF 선택 드롭다운 (등록된 PDF 목록)
- PDF 업로드 버튼 (새 PDF 추가)
- 질문 입력
- 답변 출력
- PDF 정보 표시 (선택된 PDF)
```

---

## 📝 작업 내용 상세

### Step 1: 폴더 구조 생성
1. ✅ `pdfs/` 폴더 생성
2. ✅ `vector_cache/` 폴더 생성
3. ✅ 기존 PDF 파일들을 `pdfs/` 폴더로 이동

### Step 2: 코드 구조 개선
1. ✅ `PDFManager` 클래스 구현
2. ✅ `VectorStoreManager` 클래스 구현
3. ✅ 캐싱 로직 구현 (파일 수정시간 체크)
4. ✅ 메타데이터 관리 시스템

### Step 3: 메인 코드 리팩토링
1. ✅ `Ollama_PDF_RAG.py` 개선
2. ✅ 다중 PDF 선택 기능 추가
3. ✅ Gradio UI 업데이트

### Step 4: 설정 파일 추가
1. ✅ `config.yaml` 생성
2. ✅ 폴더 경로 설정
3. ✅ 모델 설정 외부화

---

## 🎨 UI 개선 예시

### 현재 UI
```
[PDF 파일 업로드] [파일 선택]
[질문 입력]
[답변 출력]
```

### 개선 후 UI
```
┌─────────────────────────────────────┐
│ 📄 PDF 선택                          │
│ [드롭다운: 등록된 PDF 목록]          │
│                                     │
│ 📤 새 PDF 업로드                     │
│ [파일 선택 버튼]                     │
│                                     │
│ 📊 선택된 PDF 정보                   │
│ 파일명: example.pdf                 │
│ 상태: ✅ 벡터화 완료                 │
│                                     │
│ ❓ 질문 입력                         │
│ [텍스트 입력란]                      │
│                                     │
│ 💬 답변                             │
│ [답변 출력 영역]                     │
└─────────────────────────────────────┘
```

---

## 🔄 동작 흐름

### 시나리오 1: 새 PDF 업로드
1. 사용자가 PDF 파일 업로드
2. 파일이 `pdfs/` 폴더로 이동
3. PDF 해시 계산 및 메타데이터 저장
4. 벡터화 진행 (진행 상태 표시)
5. `vector_cache/{hash}/` 폴더에 벡터 DB 저장
6. PDF 목록에 추가

### 시나리오 2: 기존 PDF로 질문
1. 사용자가 드롭다운에서 PDF 선택
2. 메타데이터 확인 (파일 수정시간 체크)
3. 캐시된 벡터 DB가 있으면 로드
4. 없거나 수정되었으면 재벡터화
5. 질문에 대한 답변 생성

### 시나리오 3: 여러 PDF 관리
1. 여러 PDF를 `pdfs/` 폴더에 저장
2. 각 PDF마다 독립적인 벡터 DB 유지
3. 필요시 특정 PDF의 벡터 DB 삭제 가능

---

## 📊 예상 개선 효과

| 항목 | 현재 | 개선 후 |
|------|------|---------|
| PDF 관리 | 단일 PDF만 | 다중 PDF 동시 관리 |
| 벡터 저장소 | 덮어씌워짐 | PDF별 독립 관리 |
| 파일 정리 | 루트에 산재 | `pdfs/` 폴더로 정리 |
| 재사용성 | 매번 재벡터화 | 스마트 캐싱 |
| 사용자 경험 | 파일 선택만 | PDF 목록 + 선택 |

---

## ⚠️ 주의사항

### 파일 이동
- 기존 PDF 파일들을 `pdfs/` 폴더로 이동
- 기존 `chroma_pdf_cache/` 폴더는 유지하되, 새로운 구조로 전환

### 하위 호환성
- 기존 코드와의 호환성 유지
- 기존 PDF 파일도 정상 작동

### 데이터 보존
- 기존 벡터 캐시는 유지
- 필요시 마이그레이션 스크립트 제공

---

## ✅ 구현 체크리스트

### 폴더 구조
- [ ] `pdfs/` 폴더 생성
- [ ] `vector_cache/` 폴더 생성
- [ ] 기존 PDF 파일 이동

### 코드 구현
- [ ] `PDFManager` 클래스
- [ ] `VectorStoreManager` 클래스
- [ ] 캐싱 로직
- [ ] 메타데이터 관리
- [ ] 메인 코드 리팩토링

### UI 개선
- [ ] PDF 선택 드롭다운
- [ ] PDF 업로드 기능
- [ ] PDF 정보 표시
- [ ] 진행 상태 표시

### 설정 및 문서
- [ ] `config.yaml` 생성
- [ ] README 업데이트
- [ ] 코드 주석 추가

---

## 🚀 작업 시작 전 확인사항

**승인하시면 다음 작업을 진행합니다:**

1. ✅ 폴더 구조 생성 (`pdfs/`, `vector_cache/`)
2. ✅ 기존 PDF 파일들을 `pdfs/` 폴더로 이동
3. ✅ 새로운 클래스 및 모듈 구현
4. ✅ 메인 코드 개선 및 리팩토링
5. ✅ Gradio UI 업데이트 (다중 PDF 선택)
6. ✅ 설정 파일 생성
7. ✅ 테스트 및 검증

**예상 소요 시간**: 약 45분
**기존 기능 유지**: ✅ 모든 기존 기능은 그대로 동작
**데이터 보존**: ✅ 기존 PDF 파일 및 벡터 캐시 보존

---

## ❓ 승인 요청

**위 계획대로 작업을 진행해도 될까요?**

승인해주시면:
1. 폴더 구조 생성 및 파일 이동
2. 코드 개선 및 다중 PDF 처리 기능 구현
3. UI 업데이트

을 시작하겠습니다! 🚀

