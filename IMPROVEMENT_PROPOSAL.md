# PDF_RAG 프로젝트 개선안 상세 제안서

## 📋 현재 코드의 주요 문제점

### 1. 🔴 **벡터 저장소 캐싱 문제 (Critical)**
```python
# 현재: 매번 새로 생성하여 이전 데이터 손실
vectorstore = Chroma.from_documents(...)  # 항상 새로 생성
```
- **문제**: 같은 PDF에 여러 질문을 하면 매번 재벡터화 (5-10초 소요)
- **영향**: 사용자 경험 저하, 불필요한 리소스 사용
- **해결**: 파일명 기반 벡터 DB 분리 + 수정 시간 체크

### 2. 🔴 **메모리 및 성능 문제**
- 매번 새로운 Embeddings 인스턴스 생성
- 벡터 저장소가 단일 디렉토리에 덮어씌워짐
- 여러 PDF를 동시에 관리 불가

### 3. 🟡 **설정 관리 부재**
- 모델명 하드코딩: `'llama3'`, `'mxbai-embed-large'`
- 청크 크기 하드코딩: `chunk_size=1000, chunk_overlap=200`
- 환경별 설정 변경 불가

### 4. 🟡 **에러 처리 부족**
- 일반적인 `Exception`만 처리
- Ollama 서버 연결 실패 시 구체적 안내 없음
- PDF 파일 유효성 검사 부족

### 5. 🟡 **로깅 시스템 부재**
- `print()` 문만 사용
- 로그 레벨 관리 불가
- 디버깅 어려움

---

## 🎯 제안하는 개선안

### Phase 1: 핵심 기능 개선 (즉시 구현 권장)

#### 1.1 벡터 저장소 스마트 캐싱
**구현 내용:**
- 파일명 해시 기반 벡터 DB 분리
- 파일 수정 시간 체크 (변경 시에만 재벡터화)
- 캐시 메타데이터 저장 (JSON)
- 여러 PDF 동시 관리 가능

**예상 효과:**
- 첫 로드 후 질문 응답 시간: 5-10초 → 1-2초
- 동일 PDF 재질문 시 즉시 응답

#### 1.2 설정 파일 분리
**구현 내용:**
- `config.yaml` 또는 `.env` 파일 생성
- 모델명, 청크 크기, 임베딩 모델 등 외부화
- Gradio UI에서 모델 선택 가능하도록 개선

**구조:**
```yaml
models:
  llm: llama3
  embedding: mxbai-embed-large

chunking:
  size: 1000
  overlap: 200

retrieval:
  top_k: 4
  similarity_threshold: 0.7
```

#### 1.3 로깅 시스템 구축
**구현 내용:**
- `logging` 모듈 사용
- 파일 로그 (`logs/app.log`) + 콘솔 로그
- 로그 레벨 설정 (DEBUG, INFO, WARNING, ERROR)
- 각 단계별 로그 기록

#### 1.4 에러 처리 강화
**구현 내용:**
- Ollama 연결 확인 함수
- PDF 파일 유효성 검사
- 구체적인 예외 타입별 처리
- 사용자 친화적 에러 메시지

---

### Phase 2: UI/UX 개선

#### 2.1 진행 상태 표시
- PDF 로딩 진행률
- 벡터화 진행률
- 답변 생성 중 표시

#### 2.2 검색 결과 미리보기
- 검색된 문서 청크 표시
- 유사도 점수 표시
- 페이지 번호 정보

#### 2.3 대화 히스토리
- 이전 질문/답변 저장
- 세션 관리

---

### Phase 3: 고급 기능

#### 3.1 검색 품질 개선
- 검색 결과 개수 조절
- 다양한 검색 전략 (MMR 등)
- 하이브리드 검색

#### 3.2 배치 처리
- 여러 질문 한 번에 처리
- CSV/JSON 파일 업로드

---

## 📁 제안하는 새로운 파일 구조

```
PDF_RAG/
├── config/
│   ├── __init__.py
│   ├── settings.py          # 설정 관리 클래스
│   └── config.yaml          # 설정 파일
├── src/
│   ├── __init__.py
│   ├── vector_store.py      # 벡터 저장소 관리 (캐싱 포함)
│   ├── document_loader.py   # 문서 로더 (검증 포함)
│   ├── rag_chain.py         # RAG 체인 로직
│   └── utils/
│       ├── __init__.py
│       ├── logger.py        # 로깅 설정
│       ├── cache.py         # 캐시 메타데이터 관리
│       └── validators.py    # 검증 함수들
├── logs/                     # 로그 파일 저장
├── cache/                    # 벡터 DB 캐시
├── Ollama_PDF_RAG.py        # 메인 애플리케이션 (개선됨)
├── requirements.txt
├── .env.example             # 환경 변수 예시
└── README.md                # 업데이트된 문서
```

---

## 🚀 구현 계획

### Step 1: Phase 1 핵심 개선 (약 30분 소요)
1. ✅ 설정 파일 시스템 구축
2. ✅ 로깅 시스템 추가
3. ✅ 벡터 저장소 스마트 캐싱
4. ✅ 에러 처리 강화
5. ✅ 메인 코드 리팩토링

### Step 2: 테스트 및 검증
- 기존 기능 동작 확인
- 캐싱 동작 확인
- 에러 시나리오 테스트

---

## 💡 예상 개선 효과

| 항목 | 현재 | 개선 후 | 개선율 |
|------|------|---------|--------|
| 동일 PDF 재질문 시간 | 5-10초 | 1-2초 | **80% 감소** |
| 설정 변경 편의성 | 코드 수정 필요 | 파일 수정 | **100% 개선** |
| 디버깅 용이성 | print()만 | 구조화된 로그 | **대폭 개선** |
| 에러 처리 | 일반적 | 구체적 | **사용자 경험 개선** |
| 다중 PDF 관리 | 불가능 | 가능 | **새로운 기능** |

---

## ❓ 승인 요청

**Phase 1 핵심 개선사항을 구현하시겠습니까?**

구현할 내용:
1. ✅ 벡터 저장소 스마트 캐싱 시스템
2. ✅ 설정 파일 분리 (config.yaml)
3. ✅ 로깅 시스템 구축
4. ✅ 에러 처리 강화
5. ✅ 코드 리팩토링 및 구조 개선

**예상 소요 시간**: 약 30분
**기존 기능 유지**: ✅ 모든 기존 기능은 그대로 동작
**하위 호환성**: ✅ 기존 PDF 파일도 정상 작동

---

## 📝 추가 제안사항

### 선택적 개선 (Phase 2 이후)
- UI 진행 상태 표시
- 검색 결과 미리보기
- 대화 히스토리
- 배치 처리 기능

이 중에서도 우선 구현할 항목이 있으시면 알려주세요!

